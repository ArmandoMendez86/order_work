# api/.htaccess

# Activa el motor de reescritura de URLs
RewriteEngine On

# Si el archivo o directorio solicitado existe físicamente, no hagas nada
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Si el archivo o directorio NO existe, redirige TODAS las peticiones a index.php
# Esto es crucial para que el router PHP maneje las rutas (e.g., /login, /workorders/all)
RewriteRule ^ index.php [L]